name: Publicar três sites no GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    steps:
      - name: Baixar o código
        uses: actions/checkout@v4
      - name: Montar os três sites
        run: |
          rm -rf _site
          mkdir -p _site/Projetos/Cenicienta _site/Projetos/Una-Noche-Mas
          cp -a Projetos/Site-Karol-Sevilla/. _site/
          cp -a Projetos/Cenicienta/. _site/Projetos/Cenicienta/
          cp -a Projetos/Una-Noche-Mas/. _site/Projetos/Una-Noche-Mas/
          touch _site/.nojekyll
      - name: Preparar arquivos dos três sites
        uses: actions/upload-pages-artifact@v3
        with:
          path: _site
      - name: Publicar no GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
